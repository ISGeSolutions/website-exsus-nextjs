// utils/generateLink.js
const generateDynamicLink = (itemId) => {
    // Replace this with your logic to generate the dynamic link URL based on itemId
    // if(itemId && itemId == 'AF') {
    //     return `/destination/africa`;
    // }

  };
  
  export default generateDynamicLink;